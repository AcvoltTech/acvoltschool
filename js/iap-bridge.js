    // ============================================
    // IAP BRIDGE — DISABLED
    // No In-App Purchases — all services are in-person
    // (instructor-led classes, proctored exams)
    // Students enroll by calling: (909) 824-4849
    // Student ID = full app access
    // ============================================
    // This file is intentionally empty.
    // All payment UI on iOS is handled by ios-app-store.js (nuclear kill)
    // and triggerPayment() in config.js (directs to office phone).
